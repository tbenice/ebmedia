<?php
/**
 * @file
 * Hooks provided by this module.
 */

/**
 * Acts on user being subscribed to a specific MailChimp group.
 */
function hook_mc_user_subscribe($user, $mc_group) {
  
}

/**
 * Acts on user being unsubscribed from a specific MailChimp group.
 */
function hook_mc_user_unsubscribe($user, $mc_group) {
  
}

/**
 * Acts on MailChimp group being created.
 */
function hook_mc_group_create($mc_group) {
  
}

/**
 * Acts on MailChimp group being deleted.
 */
function hook_mc_group_delete($mc_group) {
  
}